# Zircon 旧版 EI 对话框迁移实施与验收规范

> 状态：执行中。任何模型接手都必须先读本文和对应证据文件；禁止从当前 Godot 窗口反推旧版尺寸。

## 1. 目标与事实来源

在保留 Zircon 网络、物品、技能和窗口业务逻辑的前提下，对齐 EI 3.0 的窗口尺寸、背景帧、按钮状态帧、格子数量、内部坐标、文字区域、快捷键、打开/关闭及置顶行为。这不是给新版窗口换背景；旧版和新版控件树不同，必须逐窗重建布局再绑定现有业务。

事实优先级：

1. `Mir3-Research/docs/research/ei-ui-layout/` 反编译证据。
2. 原版 WIL/WIX 与 `Magic.exp`。
3. `Mir3-Research/Tools/mir3_client_simulator/data/*.json`。
4. 网页模拟器实现，仅用于复现证据。
5. 当前 Godot 代码只提供业务逻辑，不是布局依据。

冲突时二进制静态证据优先；`candidate` 记录必须继续标记为候选。测试资源从 `/home/tetsuya/mir3ei/LegacyEI/Data` 读取，不覆盖正式资源。

## 2. 已确认的底层问题

`DXImageControl.FixedSize` 只固定控件边界，不改变贴图绘制尺寸。WIL 窗口帧通常包含大块透明画布，不能把整张画布强制缩放，否则有效图像会变小或变形。默认规则是保持原始比例，并根据帧的有效像素边界把透明画布平移到窗口原点。`StretchImage` 只保留给证据明确要求拉伸的控件。交易 F1050 等溢出窗口单独实现。

## 3. 统一实现架构

每个窗口分两层：

- `Legacy layout`：背景、旧版按钮、格子、标签和命中矩形。
- `Binding`：把 Zircon 物品/技能数组、网络操作和回调绑定到旧版控件。

禁止继续移动新版控件来近似旧版。每窗增加旧版布局构建方法，正式游戏和测试场复用同一份控件树。

统一规则：

- 背景默认使用 `FixedSize=true, StretchImage=false`，按 WIL 有效区域原点平移并由窗口裁剪；禁止拉伸有效图像。
- 按钮明确指定图库、普通/悬停/按下帧、坐标和尺寸。
- 格子按证据的起点、行列及步进创建，不使用新版默认值。
- 关闭按钮用旧版 GameInter 帧，不混入 Interface[15]。
- 旧版窗口关闭新版 `DXWindow` chrome、默认标题栏和默认关闭按钮，避免双框、黑边和双标题。
- 同一按钮/快捷键再次触发时关闭；Esc 关闭最上层；点击窗口置顶。
- 测试场允许空数据，但必须复用正式窗口，不能另造假窗口。

## 4. 三个首批窗口的精确规格

### 4.1 人物状态/装备

证据：`status-window-render-evidence.json`、`equipment_slots.json`。

- 根尺寸 244×328。
- 属性背景 GameInter F200；装备背景 F201；初始为属性页。
- F161/162 关闭按钮 `(212,298,28,26)`。
- F171/172 视图切换按钮 `(176,264,36,36)`（`status-window-render-evidence.json` 已明确修正，旧的 286 是参数顺序误抄）。
- 已确认装备槽均为 38×38：头盔 `(177,70)`；火把 `(27,264)`；毒药 `(64,264)`；左右手镯 `(27,186)/(175,186)`；左右戒指 `(27,227)/(175,227)`；鞋 `(103,264)`。
- 已确认槽位共 8 个；未确认槽位不得沿用新版 17 槽坐标。
- 属性、纸娃娃和装备数据绑定两个旧版页面，切页不能销毁状态。

验收：F200/F201 切换正确；已确认槽中心误差≤1px；关闭、拖动、悬停及换装刷新有效。

### 4.2 背包

证据：`inventory-window-render-evidence.json`、`bag-grid-46-slot-evidence.json`。

- 根尺寸 284×324，背景 GameInter F250。
- 网格 6列×6行，起点 `(25,41)`，格子/步进 36px。
- F161/162 按钮 `(249,288,28,26)`。
- F264/265 动作按钮 `(176,262,64,20)`。
- Interface1c F267/268 是人物外观候选，禁止误当 GameInter 按钮。
- 模式：0包袱、1修补、2变卖、3储存；使用证据文字。
- 包袱模式显示 `负重:%d / 总量:%d`；金币/主要数值使用旧版文本矩形。

验收：36格完整；拿起、放下、拆分、丢弃、叠加和提示有效；切换窗口不丢物品；贴图与命中矩形一致。

### 4.3 技能书

**本节旧规格已被 2026-09-24 全量审计修订，不得作为布局实施输入。**权威状态与逐项证据见 [旧版 EI UI 全量审计](LEGACY_EI_UI_AUDIT_2026-09-23.md) 的 SKL-01..10、id14 窗口目录和当前阻塞清单。

- 窗口 id14 的主初始化证据记录根尺寸 **296×332**。wrapper `0x439250` 中的 452/380 原始参数不能单独解释成屏幕根矩形；当前 Zircon 使用 452×380 并按本机 F400 alpha bbox 锚定，只是现状，不是原版规格。F400 绘制裁剪、目标 WIL 版本身份以及与 EI 根矩形的配准仍未闭合。
- primary-static `0x43A370` 证实六个技能列表 hit RECT；它不支持当前 12 个 36×36 命中格，也不能单凭计数与类别除3的文字公式推出确切分页/列表几何。
- 当前已核的分类帧序列候选为 F450/452/454/456/458/460/462/464（普通/按下帧是否与目标版本吻合仍待验）；不要沿用本节先前给后三类重复赋 F450/452/454 的错误列表。
- F410/411、F412/413 在 primary-static 构造链中是独立帧控件候选；它们不是技能图标帧。F414–419 在本机 WIL 为空，F420–431 的数字字样用途/所属窗尚未闭合。EI 技能图标来自技能记录 `[skill+6]`，经 el85 → `MIcon.wil` 路由；逐技能帧映射未闭合。
- EI 右页 `Magic.exp` 详情绘制链已静态确认，但类别列表构造、选择 ID 到当前业务记录的映射、箭头动作、F1–F12输入语义与完整布局仍有未决项。不得以较新 Client 的 F 键行为代替 EI 证据。

**验收门槛：**先取得目标同版 EXE/WIL 并核验身份；闭合根 RECT/裁剪、列表六个 RECT 与实际记录、分类/箭头状态、EI 技能 ID→`UserMagics`/MIcon 帧映射及键盘分派后，再实现。随后逐技能核帧、选择、详情内容、分页、键盘与边界命中，并保存 EI 与 Zircon 同状态完整截图。未完成这些独立对照前，技能书整体保持未验收。

## 5. 后续窗口精确规格与顺序

下面是早期迁移记录，不是无需复核的最终尺寸表。逐窗实施前必须以 [全量审计矩阵](LEGACY_EI_UI_AUDIT_2026-09-23.md) 的最新条目为准；发生冲突时按矩阵列出的证据等级与阻塞项处理，不得把本历史规格或现有 Godot 常量当作原版事实。根尺寸/裁剪尚未闭合的窗口，应先补证再定布局。

1. 聊天：GameInter F350，根 `572×388`；关闭 F161/162 命中 `(532,350,28,26)`；频道按钮 F360/361、F362/363、F364/365、F366/367、F368/369、F370/371，分别位于 `(25,332)`、`(65,332)`、`(105,332)`、`(145,332)`、`(185,332)`、`(225,332)`，均为 36×34；滚动控制候选 F380/381 在 `(539,25)`、`(539,311)`。来源：`chat-window-render-evidence.json`。
2. 任务：GameInter F700，根 `340×440`；关闭按钮使用 F161/162，动态任务正文必须保留，不能只显示空羊皮纸。来源：`quest-window-render-evidence.json` 与 `quest-window-control-map.json`。
3. 设置：GameInter F750，根 `248×264`；关闭按钮使用 F161/162；设置页签和快捷键页必须仍然可切换。来源：`window-paint-dispatch-identity.json`、`hotkey-label-handler-consistency.json`。
4. 组队：GameInter F900，根 `256×244`；成员列表、邀请/接受/拒绝/离队命中区必须绑定现有组队网络操作。来源：`window-paint-dispatch-identity.json`、`party-window-*` 证据。
5. 坐骑：GameInter F850，根 `296×332`，原点 `(0,0)`；四个动作控件为 F860/861、F862/863、F864/865、F866/867，另有 F161/162 关闭按钮，按 primary-static 控件工厂的固定相对位置构建；状态 0 只启用“上马”，非 0 启用“遛马”，收马/遛马按钮按状态证据发送对应命令。来源：`horse-window-render-evidence.json`、`horse-window-state-evidence.json`、`horse-window-wiring-evidence.json`。F850 是坐骑窗口，不能再复用腰带窗口名义。
6. NPC：GameInter F1100，根 `552×176`，状态帧 F1101/F1102；NPC 文本、选项、商店和修理数据必须在该根内刷新。来源：`npc-window-render-evidence.json`。
7. 商店/仓库：商店 GameInter F1000，仓库/行会窗口按 `window-paint-dispatch-identity.json` 的 F600/F1000 绑定；交易区不共用仓库背景。
8. 交易：GameInter F1050，使用交易证据给出的根尺寸和双方物品/金币/确认命中区；交易没有独立字母热键，只能由交易请求/接受流程打开。来源：`hotkey-label-handler-consistency.json`。
9. 行会与提示：行会 F600；公告/提示使用 F602；必须保留成员、仓库、公告正文等动态内容，不能用黑色占位面板。

每窗开始前必须先把对应 primary-static 文件、根矩形、按钮帧、命中矩形和业务绑定写入本节，再实现和验收。

## 6. 分阶段清单

### A. 底层与测试基线

- [x] 增加 `StretchImage`，默认行为不变。
- [x] 测试场支持 `--legacy-open=<window>`。
- [x] 测试场旧版窗口禁用新版 chrome、标题栏和默认关闭按钮。
- [x] 生成 character/inventory/magic 真实运行截图并校验外框比例与贴图原点。

### B. 人物窗口

- [x] 构建 F200/F201 两页，并在审计中实际切换后恢复。
- [x] 测试场已接入 F200/F201 视图切换按钮 F171/172，坐标为 `(176,264,36,36)`。
- [x] 按 `equipment_slots.json` 放置九个已确认装备槽；未确认扩展槽暂不猜测。
- [ ] 装备格和纸娃娃复用现有数据；旧版属性文本已按证据坐标建立并绑定 PlayerStats，8 个装备槽的 enum/坐标/尺寸已加入自动审计，仍待真实登录场景检查数值、换装刷新和纸娃娃像素对齐。
- [ ] 验证切页、换装、关闭、拖动、快捷键。

### C. 背包窗口

- [x] 改成旧版 6×6、36px 网格。
- [x] 放置旧版按钮、模式文字、负重和金币的旧版坐标；修补/储存模式的业务入口仍待接入。
- [ ] 绑定拿取、放下、丢弃、拆分及 NPC 模式；已补齐 Normal/Repair/Sell/Storage 四种旧版模式状态和标签入口，出售与修理 NPC 回包已接入旧版标签，存取 NPC 回包仍待接线。
- [ ] 验证格子、刷新、按钮和快捷键。

### D. 技能窗口

- [x] 测试场删除新版箭头及新版页签布局，恢复八分类书签位置。
- [x] 建立八分类页签和 12 个 36×36 技能格，位置来自 `layout.json`；格内绑定仍待业务数据验收。
- [ ] 绑定 Magic.exp 分类及 `UserMagics`（运行时技能图标/等级已接入旧版 12 格刷新；旧版格子选中后已可通过 F1-F12/Shift+F1-F12 写回当前技能栏；现代纵向列表已在旧版模式隐藏，完整服务端回写仍待人工点击验收）。
- [ ] 验证页签、提示、熟练度和快捷键。

### E. 其余核心窗口与正式验收

- [x] 测试场聊天 F350、任务 F700、组队 F900、设置 F750 已切换到旧版有效像素区域；业务控件保留为透明点击热区。
- [x] 测试场统一默认隐藏全部窗口，避免多个窗口叠加污染截图。
- [ ] 聊天、任务、设置、组队在真实登录场景逐项验证业务数据刷新；聊天/任务旧版布局已恢复动态正文可见，并加入 `LegacyAudit` alpha 检查。已修复任务切页覆盖 F700、聊天换页覆盖内部 F201/F202/F203/F204 的状态路径，仍需真实登录逐项点击验收。
- [x] 测试场已独立建立坐骑 F850（四动作按钮、F161/162 关闭按钮和 0..3 状态门控）；正式游戏热键/服务器状态接入仍待验收。腰带仍是独立的快捷物品栏，禁止与 F850 混称。
- [ ] NPC F1100 测试场根框、正文滚动区和关闭按钮已接入并通过尺寸审计；已修复真实 `ShowPage()` 回包会退回 380×204 的问题，正文/商品/修理业务仍待真实场景验收。商城/寄售按当前开发范围由 `GameStoreEnabled=false`、`ConsignmentEnabled=false` 禁用，不作为本阶段迁移缺口。
- [x] 公告/提示 F602 已按 primary-static 接入，根 584×252，公告消息在 `--legacy-ui` 下打开 F602 并保留聊天记录；关闭、操作按钮和正文坐标通过独立审计。
- [x] 正式登录场景支持 `--legacy-open=<window>` 直达人物、背包、技能、NPC、交易、行会、仓库、设置和公告窗口，便于真实业务逐窗验收。
- [x] 交易 F1050 已切换旧版根 484×330、GameInter F1050、双方 5×6 格（位置 `(14,91)` / `(246,91)`），并通过独立布局审计；仍需双客户端真实交易流程验收。
- [x] 行会 F600 已切换旧版根 446×596、GameInter F600，保留成员/仓库/公告业务控件并通过独立布局审计；仍需真实行会数据场景验收。
- [x] 仓库紧凑分支已切换 GameInter F1001、根 205×205、4×3/12 格，格点 `(22,43)`、步长 38，并通过独立布局审计；商店 F1000 与仓库分支保持分离，仍需真实 NPC 存取回包验收。
- [x] 设置 F750 已切换旧版根 248×264、GameInter F750、F161/F162 关闭按钮；旧版模式隐藏现代五页签和现代表单，只保留 F760/F762 原版状态控件，现有设置保存/音频应用逻辑继续复用；通过独立布局审计。
- [x] HUD 测试场与正式场景的核心按钮已建立真实入口：人物、背包、技能、任务、聊天、腰带、组队、菜单/设置、小地图、退出/登出、行会、交易；旧版模式的菜单按钮直接打开 F750 设置窗口，通用 `MenuDialog` 不再伪装成旧版窗口。测试场 `LegacyAudit` 额外检查 `MiniMapDialog`。仍需人工逐按钮截图核对旧版主 HUD 的最终视觉位置。
- [ ] 完整登录测试；800×600及当前缩放分别截图。

## 7. 验证命令

```bash
cd /home/tetsuya/development/Zircon
dotnet build GodotClient/ZirconClient.csproj --no-incremental

ZIRCON_UI_DATA_PATH=/home/tetsuya/mir3ei/LegacyEI/Data \
godot-mono --path GodotClient res://Scenes/LegacyHudLayoutLab.tscn -- \
  --window --legacy-open=magic

# 真实登录场景（只在明确验证旧版核心控件树时开启）
godot-mono --path GodotClient -- \
  --server 127.0.0.1 --port 7000 --user test@test.com --pass test123 \
  --char TestHero --window --legacy-ui
```

窗口名：`character`、`inventory`、`magic`、`quest`、`chat`、`group`、`menu`、`horse`、`belt`。截图使用 `grim`。每项必须同时通过编译、独立窗口截图、模拟器对照和交互验证。

2026-09-23 已完成一次真实登录 smoke test：`--legacy-ui` 场景完成 Login、StartGame 并进入地图；日志确认 `StartGame 成功` 和 `进入游戏! 玩家: TestHero`。这只证明登录及控件树接入没有阻断进游戏，不替代各窗口的业务交互验收。

## 8. 禁止事项

- 禁止肉眼微调后把值写成“原版坐标”。
- 禁止混用当前 Interface 背景和旧版 GameInter 控件。
- 禁止缩放包含透明画布的整张 WIL 帧；应按有效区域原点平移，在证据窗口矩形内裁剪透明边缘。
- 禁止只换背景就宣称窗口迁移完成。
- 禁止验证脚本复用生产代码的错误规则形成自洽结果。
